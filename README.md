# Jump Backend Service (Node.js)
## Instructions
Edit Typescript files in `lib/` and to run use: `npm start`. This will compile all Typescript files to `dist/` and run `node dist/index.js` to start the server.